#!/bin/bash

# Sincroniza con GitHub
git fetch --all
git reset --hard origin/master

# Mueve archivos de configuración
mv /cloudclusters/ListPrecios/flask.conf /cloudclusters/config/nginx/conf.d/flask.conf
mv /cloudclusters/ListPrecios/uwsgi.conf /cloudclusters/config/nginx/conf.d/sites-enabled/uwsgi.conf

# Reinicia Nginx
supervisorctl restart nginx

# Verifica estado de servicios
supervisorctl status